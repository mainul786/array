// array declaration
var numbers = [89, 10, 88, 78, 77, 98];
var friends = ['istak', 'Najiya', 'Rajib', 'Ramiz'];

// length define array numbers of element
console.log(numbers.length);

// indexOf define element position or index
console.log(numbers.indexOf(78));

// Add array Element in Last position
console.log(numbers.push(26));

// remove array element in last position
console.log(numbers.pop());

// add element in first position of array
console.log(numbers.unshift(20));

// remove element form first index
console.log(numbers.shift());

// Condition 